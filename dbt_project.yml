name: data-5035-2026
version: 1.0.0
config-version: 2
profile: data-5035-2026
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  data-5035-2026:
    +materialized: view
